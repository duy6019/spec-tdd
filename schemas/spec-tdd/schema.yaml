name: spec-tdd
version: 1
description: >
  Two-tier spec-driven workflow with superpowers TDD.
  LIGHT tier (default): proposal + delta specs, then implement with
  superpowers:test-driven-development in-session. The apply gate is [specs], so
  the OpenSpec propose workflow stops after two artifacts.
  HEAVY tier: additionally tasks + <change-name>-plan.md, plus design when its
  criteria apply. The plan is written by superpowers:writing-plans and executed
  by superpowers:subagent-driven-development.
  Requires the superpowers plugin. Every change closes with
  `openspec archive <change-name> --yes`, which merges the delta into
  openspec/specs/.
artifacts:
  - id: proposal
    generates: proposal.md
    description: Initial proposal document outlining the change
    template: proposal.md
    instruction: >
      Create the proposal document that establishes WHY this change is needed.


      Sections:

      - **Why**: 1-2 sentences on the problem or opportunity. What problem does
      this solve? Why now?

      - **What Changes**: Bullet list of changes. Be specific about new
      capabilities, modifications, or removals. Mark breaking changes with
      **BREAKING**.

      - **Capabilities**: Identify which specs will be created or modified:
        - **New Capabilities**: List capabilities being introduced. Each becomes a new `specs/<name>/spec.md`. Use kebab-case names (e.g., `user-auth`, `data-export`).
        - **Modified Capabilities**: List existing capabilities whose REQUIREMENTS are changing. Only include if spec-level behavior changes (not just implementation details). Each needs a delta spec file. Check `openspec/specs/` for existing spec names. Leave empty if no requirement changes.
      - **Impact**: Affected code, APIs, dependencies, or systems.


      IMPORTANT: The Capabilities section is critical. It creates the contract
      between

      proposal and specs phases. Research existing specs before filling this in.

      Each capability listed here will need a corresponding spec file.


      Keep it concise (1-2 pages). Focus on the "why" not the "how" -

      implementation details belong in design.md.


      This is the foundation - specs, design, and tasks all build on this.
    requires: []
  - id: specs
    generates: specs/**/*.md
    description: Detailed specifications for the change
    template: spec.md
    instruction: >
      Create specification files that define WHAT the system should do.


      Create one spec file per capability listed in the proposal's Capabilities
      section.

      - New capabilities: use the exact kebab-case name from the proposal
      (specs/<capability>/spec.md).

      - Modified capabilities: use the existing spec folder name from
      openspec/specs/<capability>/ when creating the delta spec at
      specs/<capability>/spec.md.


      Delta operations (use ## headers):

      - **ADDED Requirements**: New capabilities

      - **MODIFIED Requirements**: Changed behavior - MUST include full updated
      content

      - **REMOVED Requirements**: Deprecated features - MUST include **Reason**
      and **Migration**

      - **RENAMED Requirements**: Name changes only - use FROM:/TO: format


      Format requirements:

      - Each requirement: `### Requirement: <name>` followed by description

      - Use SHALL/MUST for normative requirements (avoid should/may)

      - Each scenario: `#### Scenario: <name>` with WHEN/THEN format

      - **CRITICAL**: Scenarios MUST use exactly 4 hashtags (`####`). Using 3
      hashtags or bullets will fail silently.

      - Every requirement MUST have at least one scenario.


      MODIFIED requirements workflow:

      1. Locate the existing requirement in openspec/specs/<capability>/spec.md

      2. Copy the ENTIRE requirement block (from `### Requirement:` through all
      scenarios)

      3. Paste under `## MODIFIED Requirements` and edit to reflect new behavior

      4. Ensure header text matches exactly (whitespace-insensitive)


      Common pitfall: Using MODIFIED with partial content loses detail at
      archive time.

      If adding new concerns without changing existing behavior, use ADDED
      instead.


      Example:

      ```

      ## ADDED Requirements


      ### Requirement: User can export data

      The system SHALL allow users to export their data in CSV format.


      #### Scenario: Successful export

      - **WHEN** user clicks "Export" button

      - **THEN** system downloads a CSV file with all user data


      ## REMOVED Requirements


      ### Requirement: Legacy export

      **Reason**: Replaced by new export system

      **Migration**: Use new export endpoint at /api/v2/export

      ```


      Specs should be testable - each scenario is a potential test case.
    requires:
      - proposal
  - id: design
    generates: design.md
    description: Technical design document with implementation details
    template: design.md
    instruction: >
      Create the design document that explains HOW to implement the change.


      When to include design.md (create only if any apply):

      - Cross-cutting change (multiple services/modules) or new architectural
      pattern

      - New external dependency or significant data model changes

      - Security, performance, or migration complexity

      - Ambiguity that benefits from technical decisions before coding


      Sections:

      - **Context**: Background, current state, constraints, stakeholders

      - **Goals / Non-Goals**: What this design achieves and explicitly excludes

      - **Decisions**: Key technical choices with rationale (why X over Y?).
      Include alternatives considered for each decision.

      - **Risks / Trade-offs**: Known limitations, things that could go wrong.
      Format: [Risk] → Mitigation

      - **Migration Plan**: Steps to deploy, rollback strategy (if applicable)

      - **Open Questions**: Outstanding decisions or unknowns to resolve


      Focus on architecture and approach, not line-by-line implementation.

      Reference the proposal for motivation and specs for requirements.


      Good design docs explain the "why" behind technical decisions.
    requires:
      - specs
  - id: tasks
    generates: tasks.md
    description: Implementation checklist with trackable tasks
    template: tasks.md
    instruction: >
      Create the task list that breaks down the implementation work.


      **IMPORTANT: Follow the template below exactly.** The apply phase parses

      checkbox format to track progress. Tasks not using `- [ ]` won't be
      tracked.


      Guidelines:

      - Group related tasks under ## numbered headings

      - Each task MUST be a checkbox: `- [ ] X.Y Task description`

      - Tasks should be small enough to complete in one session

      - Order tasks by dependency (what must be done first?)


      Example:

      ```

      ## 1. Setup


      - [ ] 1.1 Create new module structure

      - [ ] 1.2 Add dependencies to package.json


      ## 2. Core Implementation


      - [ ] 2.1 Implement data export function

      - [ ] 2.2 Add CSV formatting utilities

      ```


      Reference specs for what needs to be built, design for how to build it.

      Each task should be verifiable - you know when it's done.
    requires:
      - specs
  - id: plan
    generates: "*-plan.md"
    description: TDD micro-step implementation plan (superpowers writing-plans)
    template: plan.md
    instruction: |
      PRECHECK: confirm `superpowers:writing-plans` appears in your available
      skills list. If it is missing, STOP and tell the user. Do NOT silently
      fall back to writing the plan yourself.

      Invoke **superpowers:writing-plans** through the agent's native skill
      mechanism.

      OUTPUT REDIRECTION (required):
      - Do NOT write to `docs/superpowers/plans/`. Write the plan directly into
        this change directory as `<change-name>-plan.md`.
      - The filename MUST be change-unique. superpowers' sdd-workspace derives
        its scratch directory from `basename <plan> .md`; a plan named plan.md
        makes every change share one workspace and overwrite each other's
        ledgers.
      - Ignore the skill's "Execution Handoff" menu at the end. This schema's
        apply phase decides the executor, not that menu.

      FORMAT IS A MACHINE CONTRACT, not a style preference:
      - Task headings MUST be `### Task N: <name>`, matching
        superpowers:writing-plans 6.2.0. The task-brief parser accepts one or
        more `#` characters but still requires `Task N` at the start of the
        heading text.
      - Every checkbox MUST start at column 0. OpenSpec's checkbox regexes
        anchor `^` directly to `[-*]`, so indented checkboxes are invisible.

      Read tasks.md for the coarse breakdown and specs/ for required behavior.
      Every task must carry the explicit cycle: write the failing test, run it
      and watch it fail, minimal implementation, run it and watch it pass,
      commit.
    requires:
      - tasks
apply:
  requires:
    - specs
  tracks: null
  instruction: |
    STOP. Do not start writing implementation code directly.

    This project runs two tiers. Follow the decision protocol below before
    implementation.

    ## Which tier

    `tasks.md` is the persisted tier marker:
    - If `tasks.md` exists, the change is HEAVY.
    - If `tasks.md` does not exist, the change is a LIGHT candidate.

    A LIGHT candidate has HEAVY signals if any of these are true:
    - The change crosses module, service, or package boundaries.
    - It adds a dependency or introduces a new architectural pattern.
    - It changes a data format, persisted schema, or external contract.
    - It requires a migration or has material security/performance risk.
    - The user explicitly requested a heavy/large change.

    If a LIGHT candidate has any HEAVY signal, STOP before implementation.
    Explain the signal and ask the user to choose one of these options:
    1. Promote to HEAVY: create `design.md` when useful, then `tasks.md` and
       `<change-name>-plan.md`; rerun apply after the user reviews them.
    2. Continue as LIGHT: record the user's decision in chat and proceed without
       creating `tasks.md`.

    Never create `tasks.md` merely to resolve the question. The user decides
    whether a candidate is promoted. If no HEAVY signal exists, proceed as
    LIGHT without asking.

    ## LIGHT tier

    1. Invoke `superpowers:test-driven-development` through the agent's native
       skill mechanism.
       If that skill is not in your available skills list, STOP and tell the
       user. Do NOT implement without it.
    2. Implement in the current session with no plan file and no subagents.
       Do not add a bridge-specific worktree rule; follow the default behavior
       of any applicable Superpowers workspace skill.
    3. The Iron Law governs every line of production code you write:
       "NO PRODUCTION CODE WITHOUT A FAILING TEST FIRST."
       Write the test, run it, watch it fail for the right reason, then implement.

    ## HEAVY tier

    1. Require `tasks.md`, the exact `<change-name>-plan.md`, and at least one
       delta spec BEFORE any workspace or commit handling. Do not accept a plan
       belonging to another change. If any required Heavy artifact is missing,
       STOP and tell the user which artifact must be created and reviewed before
       apply can be rerun. `design.md` remains optional and is not part of this
       preflight.
    2. Resolve the workspace choice BEFORE creating any bridge-requested commit:
       - First detect whether the session is already in an isolated worktree. If
         it is, use that workspace and do not create another one.
       - Otherwise ask whether the user wants an isolated worktree or wants to
         work in the current checkout. Record the answer so
         `superpowers:using-git-worktrees` can honor it without asking again.
       - If the user chooses the current checkout, do NOT commit the OpenSpec
         artifacts merely for this bridge. If the checkout is main/master,
         obtain the explicit consent required by subagent-driven-development.
       - Only if the user chooses a new worktree, inspect
         `git status --short -- openspec/changes/<name>/`. If these artifacts
         have uncommitted changes, handle the commit according to the user's
         instructions and the target project's commit policy. Never stage
         unrelated files. If the applicable policy is unclear, STOP and ask.
       - After any required artifact commit, invoke
         `superpowers:using-git-worktrees` through the agent's native skill
         mechanism. Tell it the user has already consented. Once inside the new
         workspace, verify that `tasks.md`, `<change-name>-plan.md`, and every
         delta spec exist. If any are missing, STOP instead of implementing
         from stale context.
    3. Invoke `superpowers:subagent-driven-development` through the agent's
       native skill mechanism, pointed at `<change-name>-plan.md`. Tell it to
       run through its final whole-branch review and clean up only its temporary
       SDD scratch workspace, then RETURN to this apply workflow instead of
       invoking `superpowers:finishing-a-development-branch`.
    4. REQUIRED — do not skip, and do not assume it happens by itself:
       subagent-driven-development does NOT activate test-driven-development in
       superpowers 6.2.0. Its implementer prompt only says "Write tests
       (following TDD if task says to)". So you MUST state in every dispatch
       that the task is TDD-required.
    5. Tick the tasks.md checkboxes as coarse tasks complete. No superpowers
       skill does this for you.
    6. When you dispatch the final whole-branch review, include the paths of
       every file under this change's `specs/` and require a
       requirement-by-requirement conformance check against the diff.

    ## Both tiers — verification and handoff

    1. Re-read this change's delta specs and compare every requirement and
       scenario with the implementation and tests. If they differ, STOP and
       show the user each concrete mismatch. Ask the user to decide whether to:
       - fix the implementation so it conforms to the reviewed spec; or
       - revise the spec to describe a newly approved behavior.
       Never edit an approved spec merely to match the code. After the user's
       decision, make only the chosen correction and repeat the conformance
       check plus all affected tests.
    2. Invoke `superpowers:verification-before-completion` through the agent's
       native skill mechanism. Run the target project's full test command from
       `openspec/config.yaml` and require fresh passing output.
    3. STOP after implementation and verification. Do NOT archive the change,
       do NOT invoke an agent-driven OpenSpec sync surface, and do NOT invoke
       `superpowers:finishing-a-development-branch`. Report the verification
       evidence and tell the user that the explicit next command is:
       `openspec archive <change-name> --yes`.
